initSidebarItems({"macro":[["failure","A macro to shorthand the rejection from an endpoint due to a server error. /// Should be used when you want a quick 500 response to the user. Note that a string must be provided (or used like `format!()`)."],["load_env","A macro to load configuration from the environment."],["reject","A macro to shorthand the rejection from an endpoint due to a bad request. Should be used when you want a quick 400 response to the user. Note that a string must be provided (or used like `format!()`)."]]});